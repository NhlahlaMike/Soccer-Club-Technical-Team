//---------------------------------------------------------------------------
#ifndef TPlayer_uH
#define TPlayer_uH
#include <vcl.h>
//---------------------------------------------------------------------------
class TPlayer
{
	private:
		AnsiString name;
		AnsiString birthDate;
		AnsiString position;
		int jersyNo;

	public:
	   TPlayer();
	   TPlayer(AnsiString nm,AnsiString bD,AnsiString pos,int jN);
	   int extractAge();
	   ~TPlayer();
};
#endif
