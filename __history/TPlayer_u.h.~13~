//---------------------------------------------------------------------------
#ifndef TPlayer_uH
#define TPlayer_uH
#include <vcl.h>
//---------------------------------------------------------------------------
class TPlayer
{
	private:
		AnsiString name;
		AnsiString birthDate;
		AnsiString position;
		int jersyNo;

	public:
	   TPlayer();
	   TPlayer(AnsiString nm,AnsiString bD,AnsiString pos,int jN);
	   void showForm2(TForm *myForm)
	   AnsiString getName();
	   AnsiString getBirthDate();
	   AnsiString getPosition();
	   int getJersyNo();
	   ~TPlayer();
};
#endif
